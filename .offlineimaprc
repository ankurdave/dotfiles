[general]
accounts = AnkurGmail
# , Databricks

[Account AnkurGmail]
localrepository = Local
remoterepository = AnkurDaveGmail
autorefresh = 3
postsynchook = /Users/ankurdave/repos/dotfiles/bin/offlineimap-postsync

# [Account Databricks]
# localrepository = DatabricksLocal
# remoterepository = DatabricksRemote
# autorefresh = 3
# postsynchook = /Users/ankurdave/repos/dotfiles/bin/offlineimap-postsync

[Repository Local]
type = Maildir
localfolders = ~/mail

# [Repository DatabricksLocal]
# type = Maildir
# localfolders = ~/mail/databricks

[Repository AnkurDaveGmail]
type = Gmail
maxconnections = 1
remoteuser = ankurdave@gmail.com
realdelete = no
folderfilter = lambda foldername: foldername in ['[Gmail]/All Mail', '[Google Mail]/Sent Mail']
nametrans = lambda foldername: re.sub('^\[Gmail\]/All Mail$', 'all', re.sub('^\[Google Mail\]/Sent Mail$', 'sent', foldername))
# Generate this using http://mercurial.selenic.com/wiki/CACertificates#Mac_OS_X_10.6_and_higher
sslcacertfile = /Users/ankurdave/repos/dotfiles/misc/dummycert.pem

# [Repository DatabricksRemote]
# type = Gmail
# maxconnections = 1
# remoteuser = ankur@databricks.com
# realdelete = no
# folderfilter = lambda foldername: foldername in ['[Gmail]/All Mail', '[Google Mail]/Sent Mail']
# nametrans = lambda foldername: re.sub('^\[Gmail\]/All Mail$', 'all', re.sub('^\[Google Mail\]/Sent Mail$', 'sent', foldername))
